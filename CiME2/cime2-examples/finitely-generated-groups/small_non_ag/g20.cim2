(* Example borrowed from G. Karpilosky, Schur multipliers *)
(* G20 *)

let F = word_signature "a b a' b' c c'" ;

let R = SRS F "
a^2 -> ;
b^2 -> ;
c^2 -> ;
(a b c)^2 -> ;
(a b)^3 -> ;
(a c)^3 -> ;
a a' -> ;
a' a -> ;
b b' -> ;
b' b -> ;
c c' -> ;
c' c -> ;
";

let o = length_lex (word_precedence F " c' > b' > a' > c > b > a");

#time;

let G20 = word_completion o R ;

#mem;

word_counter G20; (* should be 18 *)
