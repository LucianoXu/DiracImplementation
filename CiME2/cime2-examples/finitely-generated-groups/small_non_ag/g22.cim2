(* Example borrowed from G. Karpilosky, Schur multipliers *)
(* G22 *)

let F = word_signature "a b a' b'" ;

let R = SRS F "
a^5 -> ;
b^4 -> ;
b' a b -> a^2 ;
a a' -> ;
a' a -> ;
b b' -> ;
b' b -> ;
";

let o = length_lex (word_precedence F "b' > a' > b > a");

#time;

let G22 = word_completion o R ;

#mem;

word_counter G22; (* should be 20 *)
