(* A stupid algorithm to sort list of bounded integers.*)

let N = parameters "N";

let S = pword_signature N "a{i} | {0<= i /\ i<=N } " ;

let R1 = psrs S "a{i} a{j} -> a{j} a{i} | { 0 <= i /\ i < j /\ j<= N }";

pconfluent R1;

#quit;



