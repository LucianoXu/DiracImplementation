(*

  Claude Marché

  Example given by Kusakari Keiichirou. See also ternary.cim2

*)

[y, x]

AC [+]

+(g(x),g(y)) -> g(+(g(a),x,y)) ;

(*

let F = signature "+ : AC ; g : unary ; a : constant";

let X = vars "x y x1 x2 x3 x4 x5 x6";

let R = TRS F X "g(x) + g(y) -> g(x + y + g(a))";

termcrit "dp";
termcrit "nomarks";
termination R;
output_last_proof "";
export_trs R;

*)
