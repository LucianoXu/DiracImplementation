let V = vars "X" ;

let F = signature "
  a, c, s : 1;
  f : 2;
";

let R = HTRS {} F V "
f(s(X),X) -> f(X,a(X));
f(X,c(X)) -> f(s(X),X);
f(X,X) -> c(X);
";


termcrit "minimal";
polyinterpkind {("linear",1)};
#time on;
h_termination R;

#quit;
