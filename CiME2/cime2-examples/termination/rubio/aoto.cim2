let V = vars "X" ;

let F = signature "
  f,g : 1;
";

let R = HTRS {} F V "
f(f(X)) -> f(g(f(g(f(X)))));
f(g(f(X))) -> f(g(X));
";

termcrit "minimal" ;
timelimit "60";
polyinterpkind {("linear",1);("simple",1000)};
#time on;
h_termination R;

#quit;
