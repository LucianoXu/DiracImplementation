let Var = vars "X";

let F_833d = signature "
  a,b,c : 0;
  f : 2;
";

let test_833d = HTRS {} F_833d Var "
  f(X,X)  ->  f(a,b);
  b  -> c;
";

termcrit "dp" ; 
termcrit "minimal" ; 
polyinterpkind {("linear",1)} ;
#time on;
h_termination  test_833d;

#quit;

