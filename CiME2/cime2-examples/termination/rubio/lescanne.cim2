let V = vars "X Y Z" ;

let F = signature "
  e : 0;
  i : 1;
  div : 2;
";

let R = HTRS {} F V "
div(X,e) -> i(X);
i(div(X,Y)) -> div(Y,X);
div(div(X,Y),Z) -> div(Y,div(i(X),Z));
";

termcrit "minimal";
polyinterpkind {("linear",1)};
#time on;
h_termination R;

#quit;
