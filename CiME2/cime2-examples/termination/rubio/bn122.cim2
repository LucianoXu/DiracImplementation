let Var  = vars "X Y Z";

let F = signature "
s : 1;
plus, times : 2;
";

let R = HTRS {} F Var "
  plus(plus(X,Y),Z) -> plus(X,plus(Y,Z));
  times(X,s(Y)) -> plus(X,times(Y,X));
";

termcrit "minimal";
polyinterpkind {("linear",1)};
#time on;
h_termination R;

#quit;
