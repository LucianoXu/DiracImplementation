let V = vars "X" ;

let F = signature "
  h,g : 1;
  f : 2;
";

let R = HTRS {} F V "
f(g(X)) -> g(f(f(X)));
f(h(X)) -> h(g(X));
";

termcrit "minimal";
polyinterpkind {("linear",1)};
#time on;
h_termination R;

#quit;
