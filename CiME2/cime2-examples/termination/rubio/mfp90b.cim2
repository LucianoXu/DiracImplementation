let V = vars "X" ;

let F = signature "
  a, b ,c : 0;
  g : 1;
  f : 2;
";

let R = HTRS {} F V "
a -> g(c);
g(a) -> b;
f(g(X),b) -> f(a,X);
";

termcrit "minimal";
polyinterpkind {("linear",1)};
#time on;
h_termination R;

#quit;
